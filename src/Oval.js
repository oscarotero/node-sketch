const Layer = require('./Layer');

/**
 * Represents an oval shape
 *
 * @extends {Layer}
 * @see {@link Polygon}
 * @see {@link Rectangle}
 * @see {@link Star}
 * @see {@link ShapePath}
 * @see {@link Triangle}
 */
class Oval extends Layer {}

module.exports = Oval;
