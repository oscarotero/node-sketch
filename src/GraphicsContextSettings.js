const Node = require('./Node');

/**
 * Represents a graphic context settings element
 *
 * @extends {Node}
 */
class GraphicsContextSettings extends Node {}

module.exports = GraphicsContextSettings;
