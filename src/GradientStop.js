const Node = require('./Node');

/**
 * Represents a gradient stop inside a gradient
 *
 * @extends {Node}
 * @see {@link Gradient}
 */
class GradientStop extends Node {}

module.exports = GradientStop;
