const Node = require('./Node');

/**
 * Represents a container of shared styles
 *
 * @extends {Node}
 * @see {@link SharedStyle}
 */
class SharedStyleContainer extends Node {}

module.exports = SharedStyleContainer;
